from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this is end to end resume screening and matching project.',
    author='mdzaid382',
    license='',
)
